version: 2.1

jobs:
  job_one:
    docker:
      - image: cimg/ruby:3.8.2  # Use the Ruby Docker image
    steps:
      - checkout  # Checkout the code
      - run:
          name: "Echo first hello"
          command: echo "A first hello"
      - run:
          name: "Create workspace and file"
          command: |
            mkdir -p my_workspace
            echo "Trying out workspaces" > my_workspace/echo-output
      - persist_to_workspace:
          root: my_workspace  # The root folder where the file is created
          paths:
            - echo-output  # The file to persist in the workspace

  job_two:
    docker:
      - image: cimg/ruby:3.8.2  # Use the Ruby Docker image
    steps:
      - checkout  # Checkout the code
      - attach_workspace:
          at: /workspace  # Attach the workspace to /workspace directory
      - run:
          name: "Check if workspace content is available"
          command: |
            if [ "$(cat /workspace/my_workspace/echo-output)" = "Trying out workspaces" ]; then
              echo "It worked"
            else
              echo "It failed" && exit 1
            fi

workflows:
  version: 2
  build:
    jobs:
      - job_one
      - job_two:
          requires:
            - job_one
