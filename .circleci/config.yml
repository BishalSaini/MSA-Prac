version: 2.1

jobs:
  job_one:
    docker:
      - image: cimg/ruby:2.6.8  # Ruby Docker image 2.6.8
    steps:
      - checkout  # Checkout the code
      - run:
          name: "Echo Hello World"
          command: echo "First Hello World... by Vishu"
      - run:
          name: "Sleep"
          command: sleep 25
      - run:
          name: "Authenticate using username and password"
          command: |
            echo "Authenticating with username: vishal32saini@gmail.com and password: vishu032saini"
            # Example of using the username and password to authenticate
            curl -u vishal32saini@gmail.com:vishu032saini <login-url>  # Replace <login-url> with the actual URL you're authenticating against.

  job_two:
    docker:
      - image: cimg/ruby:3.8.2  # Ruby Docker image 3.8.2
    steps:
      - checkout  # Checkout the code
      - run:
          name: "Echo Hello World"
          command: echo "Second Hello World... by Vishu"
      - run:
          name: "Authenticate using username and password"
          command: |
            echo "Authenticating with username: vishal32saini@gmail.com and password: vishu032saini"
            # Example of using the username and password to authenticate
            curl -u vishal32saini@gmail.com:vishu032saini <login-url>  # Replace <login-url> with the actual URL you're authenticating against.

workflows:
  version: 2
  build:
    jobs:
      - job_one
      - job_two
